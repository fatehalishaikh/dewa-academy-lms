'use client'
import { CurriculumLayout } from '@/views/curriculum/curriculum-layout'
export default function Layout({ children }: { children: React.ReactNode }) {
  return <CurriculumLayout>{children}</CurriculumLayout>
}
